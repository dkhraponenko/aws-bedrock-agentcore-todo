[project]
name = "todo-agent"
version = "0.1.0"
description = "Agent tools backing a DynamoDB-stored todo list"
requires-python = ">=3.13"
# The Lambda runtime already ships boto3, and nothing else is imported at
# runtime — so the deployment package is just the source tree.
dependencies = []

[dependency-groups]
dev = [
  "boto3>=1.35",
  "moto[dynamodb]>=5.0",
  "mypy>=1.13",
  "pre-commit>=4.0",
  "pytest>=8.3",
  "pytest-cov>=6.0",
  "pytest-env>=1.1",
  # Pinned exactly to the pre-commit hook's rev, not merely upper-bounded:
  # 0.15.22 already rewrites `# noqa:` suppressions into a syntax that rev
  # rejects, so a range wide enough to reach it fails the repo's own hooks.
  "ruff==0.15.12",
]

[tool.ruff]
target-version = "py313"
line-length = 120
indent-width = 4
src = ["src", "tests", "scripts"]
# docs/build_diagram.py is a one-shot image generator whose body is an SVG
# document; line length and argument counts describe the drawing, not the code.
extend-exclude = ["docs"]

[tool.ruff.lint]
select = ["E", "F"]
extend-select = [
  "W", "C90", "I", "N", "UP", "ANN", "ASYNC", "S", "BLE", "B", "A", "COM", "C4",
  "DTZ", "T10", "EM", "EXE", "FA", "ISC", "ICN", "G", "INP", "PIE", "PT", "Q",
  "RSE", "RET", "SIM", "TID", "TCH", "ARG", "PTH", "PL", "TRY", "FLY", "PERF",
  "FURB", "LOG", "RUF",
]
ignore = ["ISC001", "COM812"]
fixable = ["ALL"]
preview = true

[tool.ruff.lint.per-file-ignores]
# S101/PLR2004: bare asserts and literal expectations are the point of a test.
# PLR6301:      test methods grouped in a class never use self.
# ANN401:       **overrides on the test data factories is genuinely dynamic.
# ARG001:       fixtures requested purely for their side effects (aws_credentials).
"tests/**" = ["S101", "PLR2004", "PLR6301", "ANN401", "ARG001"]

[tool.ruff.lint.isort]
lines-after-imports = 2

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.mccabe]
max-complexity = 10

[tool.ruff.lint.pylint]
max-args = 5
max-branches = 12
max-locals = 15
max-statements = 50

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
docstring-code-format = true

[tool.mypy]
python_version = "3.13"
mypy_path = "src"
ignore_missing_imports = true
follow_imports = "silent"
no_implicit_optional = true
strict_optional = true
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
check_untyped_defs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
disallow_subclassing_any = true
allow_untyped_globals = false
allow_redefinition = false
strict_equality = true
show_error_codes = true
exclude = ["docs"]

[tool.pytest.ini_options]
# "." so tests can import the plain helper functions out of tests/conftest.py.
pythonpath = ["src", "."]
testpaths = ["tests"]
# --cov here rather than only in CI: the coverage gate is part of "the tests
# pass", and a threshold nobody runs locally is a threshold that drifts.
addopts = "-m 'not e2e' --strict-markers --cov"
markers = ["e2e: hits real AWS; excluded from the default run"]

[tool.pytest_env]
AWS_DEFAULT_REGION = "us-east-1"
TODO_TABLE_NAME = "todo-agent-items-test"
# todo_runtime validates its settings at import, which is what production wants
# and what a test run has to satisfy. Supplying them here keeps the alternative
# — a test-detection branch in the module itself — out of the source.
GATEWAY_URL = "https://gateway.invalid/mcp"
MEMORY_ID = "memory-test"
AGENT_MODEL = "us.amazon.nova-pro-v1:0"
AGENT_INSTRUCTION = "You are a todo assistant."

[tool.coverage.run]
# Only the deployment packages. scripts/ is developer tooling that no test
# imports; counting it would report a number about the wrong code.
source = ["src/todo_agent", "src/todo_runtime", "src/todo_logging"]
branch = true

[tool.coverage.report]
show_missing = true
skip_covered = true
# The handler is small enough that a real gap is a missing test, not noise —
# so this sits just under the current number rather than at a token 80.
fail_under = 95
exclude_lines = [
  "pragma: no cover",
  "if TYPE_CHECKING:",
  'if __name__ == "__main__":',
  "raise NotImplementedError",
  "\\.\\.\\.",
]
