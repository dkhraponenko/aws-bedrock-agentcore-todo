# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
infrastructure/build/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editors / OS
.DS_Store
.idea/
.vscode/

# Generated by scripts/sync_env.sh: a cached copy of terraform outputs, and the
# ARN in it carries the account id. .env.example is the committed shape.
.env

# Agent tooling
.claude/settings.local.json

# Logs downloaded from a workflow run; GitHub names them logs_<run id>
logs*/

# The diagram's PNG is rendered to be looked at; the committed SVG is what the
# README shows, and both come back from docs/architecture_diagram.py.
docs/*@2x.png
