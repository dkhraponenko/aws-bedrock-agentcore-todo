"""Real components wired together.

Offline by default - moto, a real socket, the real Lambda entry point - and the
one module marked `aws` reaches the deployed gateway instead. Both belong here
for the same reason: what they test is the wiring between parts, not a part.
"""
